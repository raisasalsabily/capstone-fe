import React from "react"

const SectionTitle = ({ title }) => {
  return (
    <div className="mt-6 mb-4 text-b-lg md:text-lg font-bold">
      <h4>{title}</h4>
    </div>
  )
}

export default SectionTitle
